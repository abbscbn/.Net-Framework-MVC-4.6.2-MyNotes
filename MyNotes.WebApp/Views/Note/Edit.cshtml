@model MyNotes.Entities.Note

@{
    ViewBag.Title = "Not Düzenleme";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-8 col-md-10 col-sm-12">

            <div class="card shadow-sm bg-warning">
                <div class="card-header text-dark">
                    <h4 class="mb-0">
                        <i class="fa fa-edit"></i> Tarif Düzenleme
                    </h4>
                </div>

                <div class="card-body">
                    @using (Html.BeginForm("Edit", "Note", FormMethod.Post, new { enctype = "multipart/form-data" }))
                    {
                        @Html.AntiForgeryToken()
                        @Html.HiddenFor(m => m.Id)
                        @Html.HiddenFor(m => m.OwnerId)

                        @Html.ValidationSummary(false)

                        <!-- Title -->
                        <div class="mb-3">
                            @Html.LabelFor(m => m.Title, new { @class = "form-label fw-bold" })
                            @Html.EditorFor(m => m.Title, new
                            {
                                htmlAttributes = new
                                {
                                    @class = "form-control",
                                    placeholder = "Note title"
                                }
                            })
                        </div>

                        <!-- Text -->
                        <div class="mb-3">
                            @Html.LabelFor(m => m.Text, new { @class = "form-label fw-bold" })
                            @Html.TextAreaFor(m => m.Text, 6, 30, new
                            {
                                @class = "form-control",
                                placeholder = "Note content"
                            })
                        </div>

                        <!-- Category -->
                        <div class="mb-3">
                            @Html.LabelFor(m => m.CategoryId, "Kategori", new { @class = "form-label fw-bold" })
                            @Html.DropDownList(
                                "CategoryId",
                                null,
                                "Select a category",
                                new { @class = "form-select" }
                            )
                        </div>

                        <!--  NoteImage -->
                        <div class="mb-3">
                            <div>
                                @Html.HiddenFor(Model => Model.NoteImageFilename)

                                <div class="mb-3">
                                    <label class="form-label fw-bold">Tarif Resmi</label>
                                    <input type="file"
                                           name="NoteImageFilename"
                                           class="form-control form-control-sm"
                                           accept="image/*" />
                                </div>

                                <small class="text-muted">
                                    JPG, PNG veya JPEG yükleyebilirsiniz
                                </small>

                            </div>
                        </div>

                        <!-- IsDraft -->
                        <div class="form-check form-switch mb-3">
                            @Html.CheckBoxFor(m => m.IsDraft, new { @class = "form-check-input", role = "switch" })
                            @Html.LabelFor(m => m.IsDraft, new { @class = "form-check-label ms-1" })
                        </div>



                        <!-- Buttons -->
                        <div class="d-flex justify-content-between">
                            <a href="@Url.Action("MyNotes")" class="btn btn-secondary">
                                <i class="fa fa-arrow-left"></i> Geri
                            </a>

                            <button type="submit" class="btn btn-success px-4">
                                <i class="fa fa-save"></i> Kaydet
                            </button>
                        </div>
                    }
                </div>
            </div>

        </div>
    </div>
</div>
