@model MyNotes.Entities.Note

@{
    ViewBag.Title = "Not Oluştur";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-8 col-md-10 col-sm-12">

            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">
                        <i class="fa fa-sticky-note"></i> Yeni Tarif
                    </h4>
                </div>

                <div class="card-body">
                    @using (Html.BeginForm("Create", "Note", FormMethod.Post, new { enctype = "multipart/form-data" }))
                    {
                        @Html.AntiForgeryToken()

                        @Html.ValidationSummary(false)

                        <!-- Title -->
                        <div class="mb-3">
                            @Html.LabelFor(m => m.Title, new { @class = "form-label fw-bold" })
                            @Html.EditorFor(m => m.Title, new
                            {
                                htmlAttributes = new
                                {
                                    @class = "form-control",
                                    placeholder = "Başlık Giriniz"
                                }
                            })
                        </div>

                        <!-- Text -->
                        <div class="mb-3">
                            @Html.LabelFor(m => m.Text, new { @class = "form-label fw-bold" })
                            @Html.TextAreaFor(m => m.Text, 6, 30, new
                            {
                                @class = "form-control",
                                placeholder = "Buraya Yazınız..."
                            })
                        </div>

                        <!-- Category -->
                        <div class="mb-3">
                            @Html.LabelFor(m => m.CategoryId, "Kategori", new { @class = "form-label fw-bold" })
                            @Html.DropDownList(
                                "CategoryId",
                                null,
                                "Kategori Seç",
                                new { @class = "form-select" }
                            )
                        </div>

                        <!--  NoteImage -->
                        <div class="mb-3">
                            <div>
                                @Html.HiddenFor(Model => Model.NoteImageFilename)

                                <div class="mb-3">
                                    <label class="form-label fw-bold">Tarif Resmi</label>
                                    <input type="file"
                                           name="NoteImageFilename"
                                           class="form-control form-control-sm"
                                           accept="image/*" />
                                </div>

                                <small class="text-muted">
                                    JPG, PNG veya JPEG yükleyebilirsiniz
                                </small>

                            </div>
                        </div>

                        <!-- IsDraft -->
                        <div class="form-check mb-4">
                            @Html.CheckBoxFor(m => m.IsDraft, new { @class = "form-check-input" })
                            @Html.LabelFor(m => m.IsDraft, new { @class = "form-check-label ms-1" })
                        </div>

                        <!-- Buttons -->
                        <div class="d-flex justify-content-between">
                            <a href="@Url.Action("Index")" class="btn btn-outline-secondary">
                                <i class="fa fa-arrow-left"></i> Geri
                            </a>

                            <button type="submit" class="btn btn-success px-4">
                                <i class="fa fa-save"></i> Oluştur
                            </button>
                        </div>
                    }
                </div>
            </div>

        </div>
    </div>
</div>
